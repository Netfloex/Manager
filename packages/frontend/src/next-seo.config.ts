import type { DefaultSeoProps } from "next-seo"

const SEO: DefaultSeoProps = {
	titleTemplate: "%s | Manager",
	defaultTitle: "Docker Manager",
	description: "Interface to manage containers",
}

export default SEO
